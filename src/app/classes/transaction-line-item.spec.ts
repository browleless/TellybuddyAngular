import { TransactionLineItem } from './transaction-line-item';

describe('TransactionLineItem', () => {
  it('should create an instance', () => {
    expect(new TransactionLineItem()).toBeTruthy();
  });
});
