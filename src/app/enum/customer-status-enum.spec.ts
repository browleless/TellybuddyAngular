import { CustomerStatusEnum } from './customer-status-enum';

describe('CustomerStatusEnum', () => {
  it('should create an instance', () => {
    expect(new CustomerStatusEnum()).toBeTruthy();
  });
});
