import { TransactionStatusEnum } from './transaction-status-enum';

describe('TransactionStatusEnum', () => {
  it('should create an instance', () => {
    expect(new TransactionStatusEnum()).toBeTruthy();
  });
});
