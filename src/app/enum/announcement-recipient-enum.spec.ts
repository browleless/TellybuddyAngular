import { AnnouncementRecipientEnum } from './announcement-recipient-enum';

describe('AnnouncementRecipientEnum', () => {
  it('should create an instance', () => {
    expect(new AnnouncementRecipientEnum()).toBeTruthy();
  });
});
